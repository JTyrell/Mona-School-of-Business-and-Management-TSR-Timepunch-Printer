# Task Tracker

- [x] 1. Initialize Project Environments
  - [x] 1.1 Create `frontend` using Vite (React).
  - [x] 1.2 Create `backend` directory.
- [x] 2. Setup Backend API (FastAPI)
  - [x] 2.1 Refactor `timesheet_bot.py` to support programmatic execution (`process_timesheets`).
  - [x] 2.2 Create `main.py` as FastAPI server.
  - [x] 2.3 Add `POST /api/generate` endpoint (handles file upload, runs bot, returns ZIP).
  - [x] 2.4 Add `POST /api/print` endpoint (attempts win32print, handles fallback response).
- [x] 3. Setup Frontend Application (React/Vite)
  - [x] 3.1 Configure styling (`index.css`) to match MSBM colors (#ac1928, #2341a4, #697ec1, #ffffff).
  - [x] 3.2 Build FileUpload component for Excel files.
  - [x] 3.3 Build Options form (Initials, Hourly Rate, Headless toggle).
  - [x] 3.4 Build progress state handler (Spinner/Loader during PDF generation).
  - [x] 3.5 Build Post-generation UI (Download ZIP, Print All).
- [x] 4. Printing & Integration Logic
  - [x] 4.1 Implement `window.print()` iframe fallback in React.
  - [x] 4.2 Connect React UI to FastAPI endpoints via fetch.
- [/] 5. Testing & Verification
  - [ ] 5.1 Verify FastAPI server starts properly.
  - [ ] 5.2 Verify React dev server builds and displays appropriately.
  - [ ] 5.3 Verify the system correctly runs the bot both visibly and deeply hidden (headless).
